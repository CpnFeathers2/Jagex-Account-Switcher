More info to be released soon.

Basically get dotnet. Open terminal in the folder of the repo. 
type "dotnet run"
Any error that comes up troubleshoot with any ai they'll fix in seconds. 

Put credential property files in Configurations and it'll auto format.. it Likes them like credentials.property.0000 and credentials.0000.property.

doesn't work with names only numbers. You can make a quick script to change all the names to numbers though. 